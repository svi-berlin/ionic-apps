import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { HomefinishedPage } from './homefinished';

@NgModule({
  declarations: [
    HomefinishedPage,
  ],
  imports: [
    IonicPageModule.forChild(HomefinishedPage),
  ],
})
export class HomefinishedPageModule {}
